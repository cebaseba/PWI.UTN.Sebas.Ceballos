let posteos = {id: 9, titulo: "mesa", autor: "cualquiera", categoria: "loquevenga" };
console.log(posteos.id + " " + posteos.titulo + " " + posteos.autor + " " + posteos.categoria);

for (let i=0; i < posteos.length;i++);

